# Changelog

## [2.5.3] - 2026-08-28

### Changed

- **MIT フォークとして公開**。挙動の変更はなくドキュメントとライセンス表記のみ。
- `LICENSE` に原作 (Lafa2K) の著作権表示を保持したまま、改変分の著作権表示を追記。
- `README.md` の Credit を拡充。上流リポジトリへのリンク、上流からそのまま引き継いで
  いるバイナリ素材の一覧 (`stream/chevrons.ytd` / `audiodirectory/custom_sounds.awc` /
  `data/signal-power-up_sounds.dat54.rel` / `gpsgeoanim.config.lua`)、および
  本フォークで追加・改変した箇所を明記した。
  素材が上流と同一であることは git blob SHA の一致で確認済み。

## [2.5.2] - 2026-08-26

### Fixed

- **`/gpspreset <存在しない番号>` が成功扱いになっていた**
  2.5.1 は KVP 保存側 (`editor.lua` の `sanitize`) だけを直しており、
  `applyRoutePreset` 自体は範囲外を現在値へ丸めて `true` を返すままだった。
  そのためコマンドは `notify.presetInvalid` に到達せず、`/gpspreset 99` が
  「プリセット0を適用」と表示していた (実機検証で確認)。
  打ち間違いに気づけないうえ、要求していないプリセットの再適用によって
  テクスチャ辞書の読み直しとルート再構築が無駄に走っていた。
  番号を明示的に渡された場合は丸めずに失敗を返すようにした
  (`next` / `prev` / KVP 復元など非数値・内部経路の挙動は不変)。
  `exports.SetRoutePreset` も同様に、存在しない番号で `false` を返す。

## [2.5.1] - 2026-08-01

### Fixed

コード全体の洗い出し (機能 / 安全性 / 性能) で見つかった問題の修正。

- **存在しないプリセット番号が「上書き」として保存されていた**
  `sanitize` が範囲を見ておらず、`applyRoutePreset` 側は範囲外を現在値へ
  丸めて成功を返すため、適用されていない番号が KVP に残り、リセット表示が
  実際とずれていた。存在する番号だけ通すようにした。
- **`SetNuiFocusKeepInput(true)` を降ろしていなかった**
  `keepGameInput = true` の場合、パネルを閉じてもフラグが立ったままで
  他リソースの NUI に影響しうる。閉じるときと `onResourceStop` で明示的に
  `false` にする。
- **存在しないテクスチャ辞書を毎フレーム要求していた**
  プリセットの `textureDict` が読めない場合、描画スレッド (`Wait(0)`) から
  永久に `RequestStreamedTextureDict` を投げ続けていた。ロード判定は毎フレーム
  のまま、要求だけ 250ms 間隔にし、約 5 秒読めなければ一度だけ原因を出す。
- **「すべて既定に戻す」だけ描画例外を捕捉していなかった**
  他の経路は `renderSafe` を通るのに、ここだけ `render` を直接呼んでいた。
- **描画数に効く値に上限が無かった**
  `beamSegments` (→128) / `beamLineCount` (→16) / ▼ の分割数 (→128)。
  config の打ち間違いでフレームが落ちるのを防ぐ。

### Security

- **`l2k_geoanim:client:*` ネットイベントの `options` を検証**
  このリソースに server_scripts は無く、演出は自前では net event を使わないが、
  上流由来のハンドラが登録されたままだった。`timeScale = 0` や巨大な
  `autoDisableAfter` を渡されると演出が終わらなくなるため、
  `timeScale` (0.05〜10)、`autoDisableAfter` (0.1〜120 秒)、`radius` (1〜200m)
  を範囲に丸める。`options` がテーブルでない場合も弾く。

## [2.5.0] - 2026-08-01

### Added

- **`/gpsedit` の「表示」に AR 起動演出のトグルを追加**
  3D ルートの ON/OFF 時に車体がワイヤーフレームで光り 3D テキストが出る演出
  (原本 `l2k_gps3d` の `gpsgeoanim`) を、パネルからも切れるようにした。
  `SHIFT + U` のショートカットおよび `Config.extraAnimationsEnabled` と
  同じ状態 (`state.extraAnimationsEnabled`) を切り替える。
  - OFF にすると再生中の演出も止める。再度 ON にしたときは長いイントロ版から
    流し直す (ショートカットと同じ挙動)。
  - パネルを開いている最中なので、ショートカット版と違いワールド内の
    フィードバック表示 (`GPS FX: ON/OFF`) は出さない。
  - KVP へ保存され再接続後も復元する。復元時は **この項目を最初に適用**する
    (`pairs` の順序は不定で、後回しになると `route.enabled` の復元で演出が
    一度流れてしまうため)。

### Fixed

- README のショートカット表記が `config/config.lua` の実際のキー設定と
  食い違っていた (ドキュメントのみ。挙動は不変)。
  - 色の循環: `SHIFT + E` → **`SHIFT + Y`** (`routeColorKey = 246`)
  - AR 演出: `SHIFT + K` → **`SHIFT + U`** (`routeAnimationToggleKey = 303`)

  コード側のフォールバック値 (38 = E / 311 = K) が書かれていたが、
  `config/config.lua` が値を持っているため使われることはない。

## [2.4.3] - 2026-07-31

### Fixed

- **パネルの周囲が真っ黒な四角になる**
  `.panel` の `backdrop-filter: blur(18px)` が原因。NUI のページは背景が
  透明でゲーム画面に重ねて合成されるが、`backdrop-filter` はページの裏側
  (= 透明) しか参照できないためゲーム画面はぼけず、CEF では合成レイヤー
  全体が不透明な黒として焼かれる。黒い領域はパネルの外形を `box-shadow` の
  ぼかし半径ぶん広げた矩形と一致していた (スクショのピクセル解析で確認。
  境界にグラデーションが無く純黒 `#000` で切れているため影ではない)。
  `backdrop-filter` を削除。ゲーム画面をぼかす効果は元々得られていない。

## [2.4.2] - 2026-07-31

### Fixed

プリセット一覧のバリを除去 (2.4.1 で残っていたぶん)。

- **使用中の行の左バーが先細りする**
  `border-left: 3px` + `border-radius: 8px` で描いていたため、角で
  ボーダーが斜めに削られて三日月状のバリになっていた。角に触れない
  独立した要素 (`::before` の丸バー) として描き直した。
- **一覧の下端で行が途中で切れる / スクロールバーが角丸の内側に出る**
  一覧を入れ子スクロールにしていたのが原因。一覧自体のスクロールを
  やめ、外側 (`.panel-body`) のスクロールに任せるようにした。
  開いたときの使用中行への追従は `scrollIntoView` に変更。
- 使用中の行にホバーの反応が無かった (`.active` の背景が `:hover` に
  勝っていた) ので、専用のホバー色を追加。

## [2.4.1] - 2026-07-31

### Fixed

`/gpsedit` パネルの表示の粗を除去。

- **ボタン類だけフォントが違う**
  `button` / `input` は `font-family` を継承しないため、本文が Segoe UI なのに
  ボタンだけブラウザ既定フォントで描かれていた。`font: inherit` を指定。
- **リセットボタン (`↺`) が項目名の直後に居座る**
  選択肢・プリセット・色の行はヘッダにコントロールが無く、右寄せの起点が
  無かった。`.field-label` に `flex: 1 1 auto` を持たせて全行の右端をそろえた。
- **色ピッカーのつまみが枠外へはみ出す**
  端まで動かすと半径ぶん (7px) が枠の外に出ていた。`calc()` で内側だけを
  動くようにし、色相バーはつまみと同じ高さ (14px) に合わせた。
- **パネルの角から中身がはみ出す**
  `overflow: hidden` を追加し、`backdrop-filter` の四角いにじみ・
  スクロールバー・本文を角丸で切るようにした。
- 本文下端の余白不足とスクロールバーのトラック色を調整。

### Changed

- **リボンプリセットに名前を付けた**
  `chevron_line_06` のようなテクスチャ名がそのまま出ていたので、
  `stream/chevrons.ytd` を展開して 12 枚のテクスチャを実際に確認し、
  見た目に基づいた名前を付けた (例: `chevron_line_06` → 「シェブロン / 縁取り」)。
  プリセットに `nameKey` を追加してロケール管理下に置き、`nameKey` を持たない
  プリセット (ユーザー追加分) は従来どおり `name` をそのまま使う。
  ワールド内バナー (`hud.preset`) だけは日本語が文字化けするため ASCII の
  `name` を使い続ける。
- **プリセット選択を自前の一覧に置き換え**
  ネイティブの `<select>` は開いた一覧が OS 描画になり、テーマも当たらず
  使用中の項目を強調できなかった。インラインで開く一覧に変更し、
  **使用中のプリセットに左のアクセントバー + ✓ + 太字**を付けた。
  開いたときは使用中の行が中央に来るようスクロールする。
- **各プリセットにサムネイルを表示**
  `chevrons.ytd` (RSC7 / DXT5) を復号して 40px の PNG を書き出し、
  `web/thumb/` に同梱。名前だけでは分からない模様の違いが一覧で分かる。

## [2.4.0] - 2026-07-31

### Added

- **`/gpsedit` 設定パネル (NUI)**
  リボンとビーコンの見た目をゲーム内でライブ調整できる。外部依存なし。
  - 表示: 路面リボン ON/OFF、ビーコン ON/OFF、ルートソース切替
  - リボン: プリセット、色 (HSV ピッカー + 不透明度、USER / MISSION 別)、
    幅、浮き、模様の間隔、破線 ON/OFF・長さ・間隔、手前フェード、描画距離倍率
  - ビーコン: 色 (ビーム / ▼ / ラベル / 区切り線 / 距離)、ビーム上下端・太さ、
    ▼ の大きさ、文字サイズ、距離の計算方法、非表示距離、ラベル・区切り線の表示
  - 項目ごとのリセット (既定値へ戻す) と一括リセット (2 度押し)
  - `RegisterKeyMapping` 登録済み。ポーズメニュー > キー設定 から割り当て可
    (既定は未割り当て)。
- **設定の永続化**
  変更が止まってから 0.8 秒後にクライアント KVP へ自動保存し、次回接続時に復元する。
  スキーマのバージョンが違う保存データは破棄する (中途半端な復元を避けるため)。

### Changed

- リボンの実効設定に上書きレイヤーを追加。
  `Config.texturedRoute` → `preset.ribbon` → `/gpsedit` の上書き の順で重ねる。
  プリセットを切り替えても `/gpsedit` の調整は残る。
- ビーコン設定も同様に上書きレイヤー化 (`Config.beacon` は書き換えない)。
  `/gpsedit` で明示した色はパレット (mission / near) より優先する。
  そうしないと近距離パレット適用中に色を編集しても何も変わらないため。
- `client.lua` / `client/beacon.lua` が内部ブリッジにセッターを追加。
  **外部リソース向け exports の互換は維持**。

### Notes / 設計

- **ox_lib を使わず自前 NUI**: `inputDialog` はモーダルで確定するまで反映されず、
  見ながら詰めるという用途に合わない。また本リソースは依存ゼロを維持したい。
- **NUI からの値は信用しない**: `config/editor.lua` のスキーマ (min/max/options) で
  必ず検証してから適用する。NaN や極端な値がループ回数に効く項目へ入ると
  クライアントが固まるため。サーバー通信・金銭・アイテムは一切絡まない。
- **フォーカス取られっぱなし対策**: ページの JS が `ready` を返す前は
  パネルを開かない。描画時の例外も捕捉して必ず閉じる。

## [2.3.0] - 2026-07-31

### Fixed

- **ビームが ▼ の真下に来ない (横にずれる)**
  ビームだけを `DrawLine` でワールド描画し、▼ とテキストは
  `GetScreenCoordFromWorldCoord` の投影で配置していた。この 2 つの投影が
  一致しない環境があり (実測: 1920x1200 / 16:10 で約 24px)、
  同じワールド XY を指定しているのにビームだけ横にずれていた。
  ビームを画面座標描画に変更し、上端は ▼ が乗っている点をそのまま使い、
  下端だけを投影する方式にした。同じ投影を共有するため必ず一直線に並ぶ。

### Added

- **`beacon.beamSpace`** (`'screen'` 既定 / `'world'`)
  `'world'` で v2.2.0 までの `DrawLine` 描画 (建物に隠れる) に戻せる。
- **`beacon.beamWidth`** (0.0016 = 約 3px @1920)
  `DrawLine` は常に 1px 幅で細すぎたが、画面座標描画では太さを指定できる。
- **`beacon.projectionAspectFix`** (既定オフ・**未検証**)
  `GetScreenCoordFromWorldCoord` の X が 16:9 基準で返っているという仮説への
  補正。16:9 環境では恒等変換。ビーコンが路面リボン終端の真上に来ない場合に
  試す。上記の描画修正とは独立 (ビーコン内部の整合はどちらでも保たれる)。

### Notes

画面座標描画では深度テストが効かないため、ビームは建物越しでも見える。
▼ とテキストは元から貫通描画だったので、これで全要素の挙動がそろった。
建物に隠したい場合は `beacon.occlusionCheckEnabled = true`
(レイキャストでビーコン全体を非表示にする) を使う。

## [2.2.0] - 2026-07-30

### Added

- **直線誘導** (`Config.directGuidance`)
  道路ルートが目的地まで届いていない場合 (道路から離れた場所、地下施設など)、
  ルート終端から目的地まで直線の破線を引く。
  ルートが描画距離上限で切られているだけの場合 (目的地が単に遠い) は引かない。
- **`Config.gpsRouting.ignoreNoGpsFlag`** (既定オフ)
  GTA の GPS が既定で避ける「GPS 非対象」道路 (裏路地・未舗装路) を経路に使わせる。
  遠回りが減ることがある。クライアント全体に効くグローバル設定のため、
  リソース停止時に必ず既定へ戻す。

### Fixed

- **近距離でビーコンが視線より上に外れて見えない問題**
  フィッティングの探索下限がビーム下端 (8m) だったため、接近すると 8m 上を
  見上げる形になっていた。下限を `fitMinHeight` (2.0m) まで下げ、
  上端が下端を割る場合はビーム下端も一緒に引き下げるようにした。
- **▼ のちらつき**
  ビーム高さの二分探索を毎フレームやり直しており、カメラの微動で結果が
  数 cm 揺れて細い輪郭線が 1〜2px 振動していた。
  指数移動平均で平滑化 (`fitSmoothing`) し、探索回数も 8 → 12 回に増やした。
- `palettes.near` の色が区切り線のライムグリーンと近く接近時の変化が
  分からなかったため、シアン系に変更しラベル・距離の文字色も変えるようにした。

### Notes

経路探索そのものは GTA 本体が行っており、このリソースは
`GetPosAlongGpsTypeRoute` で計算済みルートを読んで描画しているだけ。
「遠回りする」「地下ガレージに案内されない」は本体の車両ノードネットワークと
経路探索の挙動であり、ミニマップのルート線と同じ経路になる。
ノード間の接続関係を取得するネイティブが公開されていないため、
自前の経路探索は現実的でない。

## [2.1.0] - 2026-07-30

実機スクリーンショット確認後の UI 改善。

### Added

- **画面内維持 (`keepOnScreen`)**
  目的地が画面の横や背後にあるとビーコンごと視界から消えて方角を失っていた。
  カメラ空間で方向を求めて画面端にクランプし、寄っている端に応じて
  **向きを変えた矢印** (上/下/左/右) + 距離だけの簡易表示に切り替える。
  クランプ中はワールド座標のビームが画面に映らないため省略する。
- **パレット** (`palettes.mission` / `palettes.near`)
  MISSION ルート追従時は金色、目的地まで `nearDistance`(60m) 以内に入ると緑へ。
  宣言したキーだけを上書きする方式なので、既定色との差分だけ書けばよい。
  優先順位は near > mission > 既定。
- **二重シェブロン** (`markerDoubleEnabled`、既定オフ)
- **パルス** (`pulseEnabled`、既定オフ)
  マーカーとビームのアルファを脈動させる。可読性のため文字には掛けない。
- **距離フェード** (`fadeRange`)
  `hideCloserThan` / `maxDrawDistance` の前後でアルファを落とし、
  目的地到着直前のポップを無くす。

### Fixed

- **スタックが画面上端からはみ出して距離表示が読めない問題**
  ビーム上端を固定ワールド高さ(25m)にしていたため、目的地に近づくほど仰角が
  大きくなり、上に積んだテキストが画面外へ出ていた。
  スタックの合計高さを描画前に測り、はみ出す場合は**ビーム自体を短くして**
  全体が収まる高さへ引き下げる (`fitToScreen`)。
  ビーム上端の高さは二分探索8回で求める (固定 XY に対して画面 Y は高さの
  単調減少関数なので成立する)。テキストだけを下げるとビームが ▼ を
  突き抜けるため、ビーム長を合わせる方式を採った。

### Notes

- 画面内では必ず `GetScreenCoordFromWorldCoord` (エンジンの投影) を使う。
  自前の FOV 計算で位置を出すと、エンジンがワールド座標で描くビームと
  ずれて ▼ がビームの真上に乗らなくなるため。カメラ空間計算は
  ネイティブの結果が信用できない画面外・背後に限定している。

## [2.0.0] - 2026-07-30

`l2k_gps3d` v1.2.0 を基にした改変版。リソース名を `kn_gps3d` に変更。

### Added

- **空中ウェイポイント・ビーコン** (`client/beacon.lua`)
  目的地の上空に、垂直ビーム / ▼マーカー / `WAYPOINT` ラベル / ライムグリーンの
  区切り線 / 距離表示 を描画する。
  - ビームのみワールド 3D (`DrawLine`)、テキスト類はビーム上端をスクリーン投影した
    点を基準に画面座標で配置するため、距離が変わっても文字サイズ・間隔が一定。
  - ▼マーカーは追加アセット無しで描画する (`DrawRect` の横棒積み + 斜辺トレース)。
  - ビームは下端がフェードして空中で終わる (参照画像の実測に合わせた既定値)。
    `beamBottomHeight = 0.0` で地面まで伸ばせる。
  - USER / MISSION 両ルートソースに対応。
  - 距離は直線距離 (`distanceMode = 'direct'`) とルート残距離 (`'route'`) を選択可。
  - 1000m 以上で自動的に km 表記へ切替。
  - 画面外クランプ、近距離での非表示、レイキャストによる遮蔽判定 (既定オフ) を設定可。
  - コマンド `/gps3dbeacon` で表示切替。
  - エクスポート `SetBeaconEnabled(bool)` / `IsBeaconEnabled()`。
- **路面ルートの破線モード**
  `dashEnabled` / `dashLength` / `dashGap` で連続リボンを破線に分割する。
  破線 1 つがテクスチャ 1 枚分に収まるよう UV を張るため、セグメント境界で
  分断されても模様が崩れない。
- **プリセット [12] `Blue Dash`**
  参照画像の青い破線スタイル。プリセットが `defaultColor` / `missionColor` /
  `ribbon` を持つ場合、適用時に色とリボン形状も反映する仕組みを追加
  (既存プリセット [0]-[11] は未指定のため従来どおり色を書き換えない)。
- **カラーパレット** に `Route Blue` を追加。
- **設定ファイル分離** `config/config.lua` (shared_script)。
- **ロケール分離** `locales/{locale,en,ja}.lua`。チャット通知を日本語化。
  ワールド内 3D テキストはゲーム側フォントに日本語グリフが無いと文字化けするため、
  `Config.worldTextAscii = true` で非 ASCII を検出して英語へフォールバックする。
- **開発用リソース `kn_luacheck`** (別ディレクトリ)
  `LoadResourceFile` + `load()` でクライアントスクリプトの構文をサーバー側から
  検証する。`fivem-verify.sh` の PASS は client_scripts の構文を保証しないため。

### Changed

- `author` を `Kanayu_u` に変更 (原作クレジットは README と LICENSE に保持)。
- リボンの実効設定を `Config.texturedRoute` + プリセットの `ribbon` 上書きに分離。
  `Config` 自体は実行時に書き換えない (`currentRoutePreset` を除く)。

### Fixed

- **テクスチャ位相がルート再構築・後方トリムのたびに飛ぶ問題**
  ルート原点はプレイヤーに追従して前進するため、同じワールド地点の
  ルート距離が再構築ごとに変化し、シェブロン模様がずれていた。
  各点にルート始点からの距離 (`dist`) を保持し、原点の前進量を
  `patternPhase` に累積して補正することで模様をワールドに固定した
  (`worldStablePhase = false` で従来動作に戻せる)。
- ルート延長時に継ぎ足す点の位相が不連続になる問題 (`dist` にオフセットを適用)。
- ポルトガル語の残存文字列 `ROTA ATIVA` をロケール管理下に整理。

### Notes / 未検証

以下は描画に関わるため実機確認が必要:

- ビーム (`DrawLine`) は深度テストされ建物に隠れるが、`DrawText` は貫通して
  描画されるため、目的地が建物裏にあるとき文字だけ浮く可能性がある。
  `occlusionCheckEnabled = true` で全体を隠せる (コストがあるため既定オフ)。
- 文字サイズ (`labelScale` / `distanceScale`) は参照画像からの逆算値で、
  解像度と font による差があるため一度実機で確認して微調整が必要。
- `Blue Dash` の形状は `chevrons.ytd` に角丸テクスチャが無いため近似
  (`chevron_line_03` を使用)。完全一致には CodeWalker で YTD への
  テクスチャ追加が必要。
